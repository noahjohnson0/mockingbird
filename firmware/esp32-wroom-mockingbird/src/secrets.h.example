// Copy this file to secrets.h and fill in real values.
// secrets.h is gitignored so credentials never end up in git.
//
// On Noah's setup, secrets.h is regenerated by scripts/gen-esp32-secrets.sh
// from ~/repos/.scratch/mockingbird-wifi.txt — never hand-edit.

#pragma once

#define MOCKINGBIRD_WIFI_SSID "Mockingbird"
#define MOCKINGBIRD_WIFI_PSK  "REPLACE_ME"
